// Interface for RPC library (subset)
// Load library at runtime
loadlib("libs/libkylrpc.so")

interface rpc {
  @alias("getVersion")
  string rpc_getVersion()
  
  @alias("setLogLevel")
  bool rpc_setLogLevel(level: int32)
  
  @alias("getLastError")
  string rpc_getLastError()
}
